[project]
name = "gpu-saas-control"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115,<1", "pydantic>=2.10,<3", "pyjwt[crypto]>=2.10,<3"]

[project.optional-dependencies]
test = ["httpx>=0.28,<1"]
