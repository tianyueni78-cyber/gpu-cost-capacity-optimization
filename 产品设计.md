# GPU Cost & Capacity Copilot｜产品设计

## 1. 产品定位

GPU Cost & Capacity Copilot 是面向 AI 基础设施负责人、平台团队和 FinOps 分析人员的 GPU 决策平台。它把资源、使用、账单、SLA与业务需求数据转换为可审核的事实、优化方案和收益证据。

产品采用一个品牌、一个代码仓库和四个独立网站。每个网站拥有独立入口、独立代码、独立价值和独立交付；客户也可以按 Data → Optimize → Improve 的顺序形成闭环，Forecast 按需接入。

## 2. 模块边界

### GPU Data

- 触发：客户无法可靠回答钱花在哪、资源如何使用；
- 输入：资源清单、使用记录、账单、SLA；
- 输出：审计报告、成本与使用基线、调查线索；
- 不做：不提出配置行动，不计算可实现节省。

### GPU Optimize

- 触发：客户已有可信基线，需要比较配置方案；
- 输入：GPU Data输出，或客户提供的等价可信数据；
- 输出：使用、费率、共享和容量风险机会卡，方案比较与决策包；
- 不做：不自动修改生产资源，不把潜在节省当作实际收益。

### GPU Improve

- 触发：客户已有批准行动，需要跟踪执行效果；
- 输入：行动基线、负责人、执行时间和行动后数据；
- 输出：行动台账、实际节省、SLA变化和未实现原因；
- 不做：没有可比基线时不宣称已实现收益。

### GPU Forecast

- 触发：客户需要采购、预算或扩容计划；
- 输入：足够长度的业务量、GPU使用、事件和SLA数据；
- 输出：预测区间、容量情景和采购时间建议；
- 不做：样本不足或回测不合格时不输出单点确定结论。

## 3. 统一契约与独立代码

四个平台遵循相同的分析项目、数据版本、标准字段、映射规则、数据审计、指标口径、证据来源和规则版本，但运行代码分别保存在各自目录。第一阶段允许复制少量稳定基础代码，以换取独立运行和部署；复制内容发生业务变化时，必须用各平台测试验证一致性。

客户不必先购买 GPU Data。如果已有符合准入标准的数据，其他平台可以在自己的数据接入页直接完成准入。

## 4. 平台状态流

```text
项目草稿
→ 数据待确认
→ 审计阻断 / 分析就绪
→ 机会待审核
→ 方案已批准
→ 行动执行中
→ 待验证
→ 已验证 / 部分实现 / 未实现 / 产生负面影响
```

任何结果必须能追溯到数据版本、分析期间、使用规则和来源记录。

## 5. 客户工作流

### 5.1 项目设置

确定分析期间、目标、币种、责任人、可调整范围、SLA和不可违反的业务限制，输出项目范围和决策约束。

### 5.2 数据接入与审计

上传数据、确认字段映射并建立只读数据版本。系统检查重复、空值、范围、枚举、时间、金额公式、合同条件、成本归属和跨表关联。

阻断问题禁止进入依赖该数据的模块；警告可以继续，但必须出现在后续结果和报告中。

### 5.3 当前状态

GPU Data完成成本总额、趋势、团队归属、型号与采购结构、容量、活跃度、利用率、SLA和未归属成本的对账基线。

### 5.4 优化机会

GPU Optimize分别生成：

1. 使用优化：闲置、过度分配和可验证缩容；
2. 费率优化：稳定按需负载、承诺覆盖与续约机会；
3. 容量共享：区域、型号、时间、负载与SLA均兼容的候选；
4. 容量风险：备用不足、排队、峰值和局部短缺。

每张机会卡包含对象、证据、行动、理论收益、容量与SLA影响、置信度、适用条件、人工确认项和状态。

### 5.5 方案模拟与审批

用户把机会组合成保守、平衡、最大节省或自定义方案。系统重新计算成本、容量余量与SLA风险，并阻止同一资源、互斥行动或采购与缩容之间的收益重复计算。

### 5.6 行动与收益

批准方案生成负责人、期限、操作步骤、观察指标、回滚条件和验收日期。执行后由 GPU Improve 使用可比数据验证实际节省和副作用，再返回下一轮分析。

### 5.7 预测接入

GPU Forecast在需要未来容量决策时加入。预测必须经过时间顺序回测，并以区间和情景进入方案模拟，不能直接替代业务判断。

## 6. 关键业务规则

- 总成本必须与账单净成本对账；
- 直接成本、共享成本和未归属成本分开；
- GPU库存、已分配、活跃数量和利用率不得互相替代；
- 低利用率不是浪费的充分条件；
- 所有容量行动必须检查SLA和最低备用容量；
- 合同期内不可取消的费用不得计入立即节省；
- 互斥机会只能保留一个收益；
- 理论节省、批准节省与实际节省是三个不同指标；
- 数据不足时降低结论强度，不静默填值或伪造建议。

## 7. 页面结构

```text
平台首页
├── 项目与数据
├── GPU Data：数据审计 / 当前状态 / 调查线索
├── GPU Optimize：机会池 / 方案模拟 / 决策包
├── GPU Improve：行动台账 / 收益验证
└── GPU Forecast：需求预测 / 容量情景
```

未购买模块可以显示价值说明和所需输入，但不能伪装成已经可用的功能。

四个产品网站独立部署。稳定的数据接入与审计代码可以按版本复制到各站，但页面状态与产品业务逻辑不得混用。统一品牌首页只负责产品选择、演示入口和咨询入口，不承载分析计算。

## 8. 验证标准

- 每个平台核心能力均有 FinOps Foundation、FOCUS 或成熟专业平台依据，并说明采用与未采用部分；
- 每个模块能使用标准输入独立产生自己的交付；
- 相同数据版本在各模块中的公共指标一致；
- 阻断审计会阻止相关结论；
- 每条机会能追溯到证据和规则；
- 方案能识别冲突并去除重复收益；
- 行动前后使用可比口径，实际收益可以复算；
- Forecast回测不达标时明确拒绝生成可靠预测；
- 核心金额、容量、SLA和状态流具有自动测试。

专业对标用于约束业务口径、数据结构和验收方式，不用于复制成熟平台全部功能。没有明确依据的规则必须标记为产品假设，并通过客户试点验证。

## 9. 暂不实现

- 实时GPU任务调度；
- 自动修改云资源；
- 多租户账号和在线支付；
- 公共环境保存真实账单；
- Kubernetes或云厂商自动接入；
- 无真实客户证据支持的复杂AI功能。

## 10. 商业路径

1. GPU Data单次数据诊断；
2. GPU Optimize专项优化与方案交付；
3. GPU Improve按月跟踪实际收益；
4. GPU Forecast支持季度预算和采购；
5. 客户持续使用多个模块后，再升级为订阅平台。

## 11. 专业依据

- [FinOps Foundation：Inform、Optimize、Operate](https://www.finops.org/framework/phases/)
- [AWS Well-Architected：Cost Optimization](https://docs.aws.amazon.com/wellarchitected/latest/framework/a-cost-optimization.html)
- [Google Cloud FinOps Hub](https://docs.cloud.google.com/billing/docs/how-to/finops-hub?hl=en)
- [Vantage Cost Recommendations](https://docs.vantage.sh/cost_recommendations)
- [NVIDIA Run:ai Scheduler](https://run-ai-docs.nvidia.com/saas/platform-management/runai-scheduler/scheduling/how-the-scheduler-works)
