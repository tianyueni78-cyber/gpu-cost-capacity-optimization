# GPU四站平台设计规格

## 1. 架构决策

采用一个GitHub仓库、四套可独立运行的产品代码、四个独立Web应用入口和四个独立部署网址：

```text
gpu-data/
gpu-optimize/
gpu-improve/
gpu-forecast/
```

统一品牌首页在四站完成后建设，只负责产品选择、演示和咨询入口，不重复产品功能。

不采用四仓库方案，因为正式口径与产品文档容易分散；不采用单站四菜单方案，因为无法清楚表达独立购买和独立交付。每个平台自带运行所需代码，使用统一规格和测试防止公共口径漂移。

## 2. 当前事实

- GPU Data本地Web应用代码已完成，位于`feature/gpu-data-product`；
- GPU Optimize本地Web应用代码已完成，位于`feature/gpu-optimize-product`；
- 两者尚未作为独立公开网站完成最终验收和部署；
- GPU Improve和GPU Forecast尚未开发。

“代码完成”“合并到主分支”“公开部署”是三个不同状态，产品文档不得混用。

## 3. 统一契约

四个平台必须遵循以下统一契约：

- 四类CSV读取与引导式字段映射；
- 标准字段、类型、枚举和ID规则；
- 数据审计与分析准入；
- 数据版本和分析期间；
- 成本、容量、利用率与SLA口径；
- 证据、规则版本和结果导出；
- 公共测试样例。

每个平台在自己的目录中保存实现。少量稳定基础代码可以复制，但某个平台专属的页面状态、建议逻辑和商业措辞不能复制到其他平台。

## 4. GPU Data网站

### 用户任务

判断数据是否可信，回答钱花在哪、资源如何使用、哪里值得调查。

### 页面顺序

```text
数据接入 → 数据审计 → 当前状况 → 调查线索与诊断报告
```

### 输出

- 数据质量报告；
- 可对账的成本与资源基线；
- 有证据和限制条件的调查线索；
- Markdown摘要与CSV明细。

### 禁止越界

不输出资源调整方案，不把相关成本写成可节省金额，不因低利用率直接判定浪费。

### 专业标准

遵循FinOps Data Ingestion、Allocation和Reporting & Analytics：所有金额可对账，成本归属、遥测覆盖和未分配部分透明展示。

## 5. GPU Optimize网站

### 用户任务

比较GPU配置方案，在容量与SLA约束下决定调整什么。

### 页面顺序

```text
需求与约束 → 优化机会 → 方案比较 → 审批包
```

### 输出

- 使用、费率、共享和容量风险机会卡；
- 当前方案与候选方案对比；
- 冲突与不可行原因；
- 可审批的行动包。

### 强制规则

- 当前配置和建议配置并列；
- 展示观察窗口、峰值、利用率和性能风险；
- SLA、备用容量、区域、型号与兼容性形成约束；
- 先计算缩容与闲置处理，再重新计算承诺采购；
- 互斥行动和同一资源收益不得重复累加；
- 不自动修改生产资源。

### 专业标准

参考AWS Compute Optimizer的当前/建议规格、利用率图、价格差和性能风险，以及Azure先缩容再重新计算承诺建议的顺序。

## 6. GPU Improve网站

### 用户任务

跟踪批准行动是否执行，并验证实际节省和业务副作用。

### 页面顺序

```text
行动导入 → 执行台账 → 前后对比 → 收益复盘
```

### 输出

- 行动负责人、期限和状态；
- 预计、批准、已执行和已验证节省；
- 成本、业务量、容量与SLA前后对比；
- 未实现原因和回滚建议；
- 月度收益报告。

### 强制规则

- 每项行动绑定基线数据版本；
- 行动前后必须使用可比期间和口径；
- 实际节省必须同时控制业务量、价格和范围变化；
- 没有反事实基线时标记“无法验证”；
- 执行完成不自动等于收益实现。

### 专业标准

遵循FinOps Operate的行动、度量和持续改进循环，并采用Google FinOps Hub区分潜在节省与已实现节省的口径。

## 7. GPU Forecast网站

### 用户任务

预测未来业务量、GPU容量和成本，支持预算、采购和扩容时点决策。

### 页面顺序

```text
范围与业务驱动 → 时间回测 → 需求与容量情景 → 采购预算计划
```

### 输出

- 朴素基线与候选模型回测；
- 预测区间与误差指标；
- 基准、增长、峰值和压力情景；
- 加入SLA备用后的容量需求；
- 实际与预测偏差跟踪。

### 强制规则

- 数据按时间划分训练与测试；
- 所有模型必须与朴素基线比较；
- 业务量先预测，再换算容量；
- 历史趋势与计划事件分开；
- 使用预测区间而非单点结果；
- 回测或样本不合格时拒绝输出可靠采购建议。

### 专业标准

遵循FinOps Forecasting：历史成本、业务驱动和计划变化共同进入模型，预测与实际持续比较，并为当前决策服务。

## 8. 站间接口

| 来源 | 输出接口 | 使用方 |
|---|---|---|
| GPU Data | 审计状态、成本与资源基线、警告 | Optimize、Improve、Forecast |
| GPU Optimize | 机会、方案、批准行动、预计收益 | Improve |
| GPU Improve | 已验证收益、SLA变化、失败原因 | Data、Optimize、Forecast |
| GPU Forecast | 预测区间、容量情景、误差 | Optimize |

接口使用稳定表结构或内部数据对象，不通过复制页面代码传递。

## 9. 部署与商业边界

- 每个网站有独立入口、产品说明、样例和导出；
- 第一版本地运行，公开演示只使用合成数据；
- 可以单独试用和交付，不强制购买前置模块；
- 未购买的网站不影响已购买网站运行；
- 第一版不建设账号、支付、多租户数据库和生产写入；
- 真实客户需求成立后，再选择托管平台和权限体系。

## 10. 统一验收标准

每个网站必须同时满足：

1. 新用户可以按页面提示独立走完一次任务；
2. 输入、输出和禁止越界明确；
3. 关键结果可追溯到数据和公式；
4. 错误数据不会产生看似正常的结果；
5. 核心规则具备正常、异常和边界测试；
6. 提供一套合成演示数据和一份样例报告；
7. README明确区分已完成、规划和不做；
8. 本地服务健康检查和全部测试通过；
9. 公开部署不保存客户真实数据；
10. 至少完成一次30分钟用户试用记录后才称为“已验证产品”。

## 11. 实施顺序

```text
验收并发布GPU Data
→ 验收并发布GPU Optimize
→ 设计、开发并发布GPU Improve
→ 设计、开发并发布GPU Forecast
→ 建设统一品牌首页
```

每个网站独立经历：专业对标、设计批准、TDD实施、数据验证、产品验收和部署。不得同时开发两个尚未稳定的网站。

## 12. 专业依据

- [FinOps Reporting & Analytics](https://www.finops.org/framework/capabilities/reporting-analytics/)
- [FinOps Phases](https://www.finops.org/framework/phases/)
- [FinOps Forecasting](https://www.finops.org/framework/capabilities/forecasting/)
- [AWS Compute Optimizer](https://docs.aws.amazon.com/compute-optimizer/latest/ug/view-ec2-recommendations.html)
- [Azure Advisor Savings](https://learn.microsoft.com/en-us/azure/advisor/advisor-how-to-calculate-total-cost-savings)
- [Google Cloud FinOps Hub](https://docs.cloud.google.com/billing/docs/how-to/finops-hub?hl=en)
