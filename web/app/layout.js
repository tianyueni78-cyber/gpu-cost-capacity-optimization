import "./style.css";

export const metadata = { title: "GPU Cost & Capacity Copilot" };

export default function RootLayout({ children }) {
  return <html lang="zh-CN"><body>{children}</body></html>;
}
